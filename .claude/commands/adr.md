Create a new Architecture Decision Record (ADR) for this project.

The user's decision topic: $ARGUMENTS

## Instructions

1. Look at the existing ADR files in `docs/decisions/` to determine the next number (e.g., if 005 exists, create 006)
2. Read the template at `docs/decisions/template.md` for the format
3. Have a brief conversation with the user to understand:
   - What is the context / problem?
   - What was decided and what alternatives were considered?
   - What are the positive and negative consequences?
4. Create the ADR file at `docs/decisions/NNN-kebab-case-title.md` with:
   - The next sequential number (zero-padded to 3 digits)
   - Status set to "Accepted" (or "Proposed" if the user is still deciding)
   - Today's date
   - Filled-in Context, Decision, and Consequences sections based on the conversation
5. If there is enough information in $ARGUMENTS to draft the full ADR without asking questions, go ahead and draft it, then ask the user to review

Keep the writing concise and factual. Focus on the "why" — the code shows "what", the ADR explains "why".
